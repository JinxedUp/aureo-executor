#include <iostream>

int main()
{
    std::cout << "Aureo public-executor package\n";
    std::cout << "This open-source package contains the UI and public interfaces only.\n";
    std::cout << "Closed-core runtime internals are intentionally excluded.\n";
    return 0;
}
